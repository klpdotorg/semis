$def with (sslc_district,semis_district)
<html>
<head>
</head>
<body>
<script>

</script>
<form method="post" action="go">
<select id="dist" name="dist" onchange="this.form.submit();">
	<option value="0">Select District</option>
$for dist i sslc_district:
	$if dist.id==int(values['dist'].strip()):
		<option value="$dist.id" selected="selected">$dist.name.title()</option>
	$else:
		<option value="$dist.id">$dist.name.title()</option>
</select>
<select id="blck" name="blck" onchange="this.form.submit()">
	<option value="0">Select District</option>
$for blck in semis_district: 
	$if blck.id==int(values['blck'].strip()):
        	<option value="$blck.id" selected="selected">$blck.name.title()</option>
	$else:
		<option value="$blck.id">$blck.name.title()</option>
</select>

<br><br>
<table border=1 width='50%'>
	<tr><td width='50%'>
	</td>
</form>
</body>
</html>
